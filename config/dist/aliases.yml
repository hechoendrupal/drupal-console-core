commands:
  aliases:
    debug:container:
      - cod
    config:edit:
      - cdit
    cron:execute:
      - cre
    cron:release:
      - crr
    debug:module:
      - mod
    debug:multisite:
      - msd
    debug:router:
      - rod
    debug:theme:
      - tde
    debug:update:
      - upd
    multisite:new:
      - sn
    router:rebuild:
      - ror
    user:login:clear:attempts:
      - uslca
    user:login:url:
      - usli
      - uli
    user:password:hash:
      - usph
    user:password:reset:
      - upsr
    views:disable:
      - vdi
    cache:rebuild:
      - cc
    update:execute:
      - updb
